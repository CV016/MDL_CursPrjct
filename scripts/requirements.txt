# Traffic simulator and drift dataset builder (run on your laptop or the server;
# not part of the Docker backend image).
#
# Pinned loosely so older shared servers (Python 3.7 / pandas 1.x wheels) still work.
# Prefer Python 3.9+ locally if you use build_drift_dataset.py (Hugging Face datasets).

pandas>=1.1.0,<2.0.0
datasets>=2.0.0,<3.0.0
requests>=2.25.0
tqdm>=4.50.0

# Optional: run_pipeline_batch.py --mlflow-uri
mlflow>=1.28.0,<3.0.0
